# 任务计划：USB 报警灯控制与水质仪表盘卡片

## 目标
在现有 AquaGarden Vue 前端中，依据 RA8P1 固件协议增加 USB 报警灯控制卡片，并在仪表盘传感器行增加与现有逻辑一致的水质卡片。

## 当前阶段
阶段 7

## 各阶段

### 阶段 1：需求与发现
- [x] 确认目标前端和硬件目录
- [x] 梳理 USB 报警灯命令、状态和后端映射
- [x] 梳理水质字段及仪表盘传感器渲染方式
- **状态：** complete

### 阶段 2：规划与结构
- [x] 确定最小兼容方案
- [x] 记录交互、字段和样式决策
- [x] 添加失败的接口与模式映射测试
- **状态：** complete

### 阶段 3：实现
- [x] 增加 USB 报警灯控制模块
- [x] 增加水质传感器显示卡片
- [x] 保留并兼容工作树现有改动
- **状态：** complete

### 阶段 4：测试与验证
- [x] 运行前端测试和构建
- [x] 使用真实浏览器检查桌面和移动端布局
- [x] 检查控制请求和数据显示逻辑
- **状态：** complete

### 阶段 5：交付
- [x] 汇总修改文件和验证结果
- [x] 保留按要求启动的 Vite 服务并关闭浏览器会话
- **状态：** complete

### 阶段 6：修复运行环境中的 USB 灯 404
- [x] 确认 8090 实际运行后端及已有 COM 控制入口
- [x] 为实际运行后端添加 USB 灯路由与严格模式校验
- [x] 验证前端请求可到达 COM→MCU 控制链路
- [x] 运行回归测试与接口检查
- **状态：** complete

### 阶段 7：FastAPI 传感器上下文 AI 多轮问答
- [x] 梳理仪表盘现有 AI 请求和 FastAPI AI 服务契约
- [x] 添加失败测试，覆盖传感器上下文、多轮历史、认证和密钥安全
- [x] 实现 FastAPI 内部 API Key 模型调用与规则回退
- [x] 对接并完善 Vue 聊天交互状态
- [x] 重启 FastAPI，完成接口、构建和运行验证
- [ ] 在 FastAPI 环境换入有效 Kimi Key，验证真实响应 `source=llm`
- **状态：** blocked_by_invalid_kimi_credential

## 关键问题
1. 固件暴露的 USB 报警灯控制值和后端 API 路径是什么？
2. 水质值对应 TDS 还是其他综合字段，单位和可用性规则是什么？
3. 现有 AI 路由是否已经由 FastAPI 提供，缺失的是多轮契约、运行重载还是模型调用实现？

## 已做决策
| 决策 | 理由 |
|------|------|
| 沿用现有页面设计与数据获取模式 | 减少行为差异，避免影响现有控制和实时刷新 |
| 不回退工作树已有改动 | 当前相关文件存在用户未提交修改 |
| 新增认证 API `/api/aqua/usb-light`，严格接收模式 0..26 | 固件数字模式为封闭集合，服务端必须做 allowlist 校验 |
| 串口命令必须等待新遥测帧确认 | 固件未就绪时会拒绝命令且模式不变 |
| 前端用分组选择器暴露 27 种模式并提供快捷关闭 | 模式数量较多，选择器比密集按钮更易扫描且不挤压布局 |
| 水质统一显示为 TDS 浊度 NTU，详情范围 0..1400 | 与固件曲线和协议单位一致 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| `rg` 的 Markdown 通配符路径在 Windows 无效 | 1 | 改用显式文件路径或目录检索 |
| PowerShell 区间数组读取脚本类型不匹配 | 1 | 改用 `Select-Object -Skip/-First` |
| 跨多文件补丁因计划文件上下文不匹配被拒绝 | 1 | 拆分为小补丁并按实际上下文更新 |
| 并行运行前端测试时 npm 未识别现有 `test:unit` 脚本 | 1 | 核对实际工作目录后改为分别执行 |
| Vite 默认 `dist/assets` 被占用，清理时报 EPERM | 1 | 保留现有产物，改用系统临时目录验证构建 |
| 前端后台进程启动后 5173 拒绝连接 | 1 | 读取临时日志并改用可用端口/启动方式 |
| `compileall` 无法写入现有 `__pycache__` 临时文件 | 1 | 不删除现有缓存；以完整 pytest 的实际导入执行作为验证 |
| 系统控制页调用 USB 灯返回 Not Found | 1 | 重启旧 FastAPI 进程，新 OpenAPI 已加载 USB 路由 |
| 规划恢复脚本路径不存在 | 1 | 直接读取 `task_plan.md`、`findings.md`、`progress.md` 恢复 |
| WMI 查询监听进程命令行被拒绝 | 1 | 改用非特权进程信息和 OpenAPI |
| 沙箱内停止旧 FastAPI 进程被拒绝 | 1 | 经授权停止 PID 62980 并启动新进程 |
| 沙箱内新 FastAPI 子进程退出 | 1 | 使用已授权的沙箱外后台启动方式 |
| PowerShell `$home` 临时变量不可写 | 1 | 避免保留变量名；不影响后续代理验证结果 |
| 记录补丁跨文件上下文放错 | 1 | 拆分为按文件匹配的小补丁 |
| AI 服务补丁变量插入位置偏移 | 1 | 在运行前检查并移入 `_call_anthropic()` |
| 真实 AI 调用首次返回 fallback/error | 2 | 已确认端点和 Bearer 格式正确；当前 `ANTHROPIC_AUTH_TOKEN` 被 Kimi 返回 401，需在 FastAPI 环境更换有效凭据 |

## 备注
- 外部或硬件文档原文只记录在 findings.md，不写入本计划。
