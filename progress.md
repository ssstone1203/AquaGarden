# 进度日志

## 会话：2026-07-16

### 阶段 1：需求与发现
- **状态：** complete
- **开始时间：** 2026-07-16
- 执行的操作：
  - 读取适用技能说明。
  - 检查仓库结构、目标目录和工作树状态。
  - 确认相关前端文件已有未提交修改。
  - 检索固件命令、遥测字段和后端串口映射。
  - 读取 USB 灯驱动、控制协议、现有控制页和仪表盘实现。
  - 对比工作树差异，确认需要兼容现有雾化器及视频链路改动。
  - 定位仪表盘传感器卡片、轮询、WebSocket、详情弹窗的全部接入点。
  - 确认后端尚缺 USB 灯控制路由和顶层状态映射。
  - 核对 USB 灯全部 27 种模式、前置条件和 250 ms 遥测确认机制。
- 创建/修改的文件：
  - `task_plan.md`
  - `findings.md`
  - `progress.md`

### 阶段 2：规划与结构
- **状态：** complete
- 执行的操作：
  - 确定认证 API、遥测确认、完整模式选择器和水质 NTU 卡片方案。
  - 确认测试框架和现有雾化器测试模式，准备按 TDD 添加 USB 灯测试。
  - 添加 USB 灯 API、串口确认和前端模式表测试。

### 阶段 3：实现
- **状态：** complete
- 执行的操作：
  - 开始实现 USB 灯后端控制链路和前端模式模型。
  - 完成 USB 灯控制卡片与水质卡片的数据、交互和响应式样式。

### 阶段 4：测试与验证
- **状态：** complete
- 执行的操作：
  - 运行前端单测、生产构建和后端完整测试。
  - 使用 Playwright 检查 1920x1080 与 390x844 的仪表盘和控制页。
  - 修复浏览器检查发现的未知模式误判与既有移动端溢出。

### 阶段 5：交付
- **状态：** complete
- 执行的操作：
  - 关闭 Playwright 浏览器会话。
  - 保留 Vite 开发服务 `http://127.0.0.1:5173`。

### 阶段 6：修复运行环境中的 USB 灯 404
- **状态：** complete
- **开始时间：** 2026-07-17
- 执行的操作：
  - 收到系统控制页 USB 灯请求返回 Not Found 的反馈。
  - 开始核对 8090 实际后端与 COM→MCU 路由。
  - 确认 8090 返回 Uvicorn 404，用户明确实际后端为 FastAPI。
  - 通过 netstat 确认 8090 监听 PID 为 62980。
  - 对比运行 OpenAPI 与当前源码，确认旧进程未加载 `/api/aqua/usb-light`。
  - 运行 USB 灯目标测试 10 项，全部通过。
  - 停止旧 PID 62980，启动新 FastAPI PID 17068。
  - 验证新 OpenAPI 包含 USB 灯路由，未认证请求返回 401。
  - 以沙箱外持久进程重新启动 FastAPI，监听 PID 33916。
  - 读取实际 COM20 状态并确认串口在线、遥测持续更新。
  - 发送 mode=26 完成真实 COM→MCU 往返，接口和遥测均确认成功。
  - 通过 5173 前端代理验证 USB 路由校验和 mode=26 状态读取。
  - 运行 FastAPI 完整测试 46 项，全部通过。
  - 确认 PID 33916 持续监听 8090，运行 OpenAPI 路由存在。

### 阶段 7：FastAPI 传感器上下文 AI 多轮问答
- **状态：** in_progress
- **开始时间：** 2026-07-17
- 执行的操作：
  - 收到将仪表盘 AI 问答重构到 FastAPI 的需求。
  - 开始检查前端聊天请求、实时传感器状态和后端模型调用服务。
  - 确认现有 FastAPI/前端已具备基础聊天链路，定位 TDS 单位、快照新鲜度和错误泄露问题。
  - 确认 API Key 配置类型与示例不足，并梳理前端现有多轮交互能力。
  - 确认运行路由存在并确定新增独立 AI 回归测试文件。
  - 添加 AI 快照、NTU、多轮历史、错误脱敏、SecretStr 与认证测试；实现前 1 通过/5 失败。
  - 完成 FastAPI 配置、路由和 AI 服务重构，目标测试 6/6 通过。
  - 安全检查本地模型配置已启用且内部 API Key 存在，未输出密钥内容。
  - FastAPI 全量测试 52/52、前端单测 8/8、Vite 生产构建和差异检查通过。
  - 重启 FastAPI 为 PID 14028，真实聊天已读取硬件快照，但模型调用首次回退。
  - 用户确认不再使用 Spring 逻辑；后续实现、配置和运行验证均限定在 FastAPI。
  - 新增 `AQUAGARDEN_LLM_AUTH_MODE`，Kimi 默认仅发送 Bearer，直连 Anthropic 可切换为 `x-api-key`，避免重复暴露同一秘密。
  - 新增两种鉴权头的参数化回归测试，目标测试 19/19、FastAPI 全量 54/54 通过。
  - 重启 FastAPI 为 PID 68880，JWT 聊天端到端返回 200 且使用硬件快照；当前 Kimi 凭据返回 401，接口按设计脱敏回退。
  - 前端单测 8/8、Vite 生产构建 42 模块、差异检查通过。

## 测试结果
| 测试 | 输入 | 预期结果 | 实际结果 | 状态 |
|------|------|---------|---------|------|
| 前端单元测试（实现前） | `npm run test:unit` | 新增测试失败 | `usbLightModes.js` 不存在，4 通过/1 失败 | expected-fail |
| 后端目标测试（实现前） | USB 灯 API/认证/状态/遥测确认 | 新增测试失败且原有测试通过 | 4 通过/8 失败 | expected-fail |
| 前端单元测试（模式实现后） | `node --test` | 全部通过 | 7 通过/0 失败 | pass |
| 后端目标测试（实现后） | USB 灯 API/认证/状态/遥测确认 | 全部通过 | 12 通过/0 失败 | pass |
| 后端完整测试 | `python -m pytest -q` | 全部通过 | 46 通过/0 失败 | pass |
| 前端最终单测 | `node --test` | 全部通过 | 8 通过/0 失败 | pass |
| 前端生产构建 | Vite 临时输出目录 | 构建成功 | 42 模块完成转换 | pass |
| 浏览器视觉检查 | 1920x1080、390x844 | 无卡片溢出或互相遮挡 | 仪表盘与控制页通过 | pass |
| 差异检查 | `git diff --check` | 无错误 | 无输出 | pass |
| FastAPI USB 灯目标测试 | API/鉴权/帧/遥测确认 | 全部通过 | 10 通过/0 失败 | pass |
| FastAPI 完整回归 | `python -m pytest -q` | 全部通过 | 46 通过/0 失败 | pass |
| FastAPI AI 目标测试（实现前） | `test_ai_chat.py` + 认证 | 新增行为测试失败 | 1 通过/5 失败 | expected-fail |
| FastAPI AI 目标测试（实现后） | `test_ai_chat.py` + 认证 | 全部通过 | 6 通过/0 失败 | pass |
| FastAPI AI 全量回归 | `python -m pytest -q` | 全部通过 | 52 通过/0 失败 | pass |
| AI 前端单测与构建 | `node --test` + Vite | 通过 | 8 通过，42 模块构建成功 | pass |
| FastAPI AI 鉴权模式测试 | Bearer / x-api-key | 密钥只进入一个请求头 | 2 通过/0 失败 | pass |
| FastAPI 最终完整回归 | `python -m pytest -q` | 全部通过 | 54 通过/0 失败 | pass |
| FastAPI AI 端到端 | JWT + 实时硬件快照 | 接口 200 且不泄密 | hardware 快照、fallback/error | partial（Kimi Key 401） |
| 实际 COM→MCU 安全命令 | mode=26 | 遥测确认关闭模式 | 200 confirmed，遥测 mode=26 | pass |

## 错误日志
| 时间戳 | 错误 | 尝试次数 | 解决方案 |
|--------|------|---------|---------|
| 2026-07-16 | `rg` 使用 Windows 不兼容的 Markdown 通配符路径 | 1 | 后续改用显式路径或目录检索 |
| 2026-07-16 | PowerShell 区间数组参数类型不匹配 | 1 | 改为固定 `Select-Object` 区间读取 |
| 2026-07-16 | 跨多文件补丁上下文不匹配 | 1 | 按实际文件拆成小补丁 |
| 2026-07-16 | 并行测试时 npm 报告缺少 `test:unit` | 1 | 检查工作目录与包脚本后分别执行 |
| 2026-07-16 | `package.json` 在任务期间改为 `test:controls` | 1 | 保留外部改动，使用 `node --test` 运行全部测试 |
| 2026-07-16 | Vite 清理 `dist/assets` 时权限被拒绝 | 1 | 改用临时输出目录，不触碰被占用的现有产物 |
| 2026-07-16 | Vite 后台进程启动后立即不可访问 | 1 | 检查临时日志和进程状态 |
| 2026-07-16 | `compileall` 写现有 `__pycache__` 时权限被拒绝 | 1 | 保留缓存目录；完整测试已成功导入相关模块 |
| 2026-07-17 | USB 灯控制请求返回 Not Found | 1 | 排查运行后端与路由注册不一致 |
| 2026-07-17 | 规划恢复脚本不存在 | 1 | 直接读取现有规划文件恢复上下文 |
| 2026-07-17 | WMI 查询监听进程命令行被拒绝 | 1 | 使用 OpenAPI 与普通进程信息继续诊断 |
| 2026-07-17 | 沙箱内无法停止旧 FastAPI PID 62980 | 1 | 经授权在沙箱外停止并重启 |
| 2026-07-17 | 沙箱内启动的新 FastAPI 子进程退出 | 1 | 改为沙箱外持久后台进程；此次未发送硬件命令 |
| 2026-07-17 | PowerShell `$home` 与只读 HOME 变量冲突 | 1 | 后续不再使用该临时变量；其余代理验证成功 |
| 2026-07-17 | 记录补丁跨文件上下文错误 | 1 | 拆分小补丁后继续 |
| 2026-07-17 | `api_key` 变量插入到 AI 服务文件末尾 | 1 | 测试前移入 Anthropic 调用函数 |
| 2026-07-17 | 真实模型请求返回 fallback/error | 2 | 已确认 Kimi Bearer 请求返回 401 authentication_error；需在 FastAPI 环境更换有效 Key |

## 五问重启检查
| 问题 | 答案 |
|------|------|
| 我在哪里？ | 阶段 7：FastAPI AI 问答重构 |
| 我要去哪里？ | 完成传感器驱动的安全多轮问答与运行验证 |
| 目标是什么？ | 增加 USB 报警灯控制和水质传感器卡片 |
| 我学到了什么？ | 见 findings.md |
| 我做了什么？ | 见上方记录 |
