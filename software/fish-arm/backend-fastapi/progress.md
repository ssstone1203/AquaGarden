# 实施进度

## 2026-07-15
- 已确认用户目标与本机 COM20 设备。
- 已读取项目概览、配置、串口和 WebSocket 入口。
- 已启用安全、测试、前端设计和文件规划工作流。
- 当前进行：完整审查后端与 front-vue 实现。
- 已定位实时广播的跨事件循环问题，并确认前端已有传感器消息处理基础。
- 已写入实时消息契约、WebSocket 初始快照和跨线程广播测试，下一步确认测试按预期失败。
- 红灯测试已确认失败点；已实现 COM20 默认配置、线程安全广播和前端兼容事件。
- 3 条实时链路测试已通过。
- 已补充 COM20 `.env.example`、WebSocket 消息契约和当前 `main.py` 启动说明。
- 首次全量编译因现有 `__pycache__` 权限失败，准备改用临时缓存目录验证。
- 前台硬件实测确认 COM20 帧解析成功，并定位 SQLite 只读错误会中断串口的问题；已添加回归测试。
- 已实现持久化故障隔离及通用 `lastPersistError` 状态。
- COM20 重测 2 秒内帧计数从 1 增至 9，连接持续、CRC/无效帧均为 0；首次连续 WebSocket 检查因未等待串口就绪而超时，准备按硬件就绪条件重测。
- 真实 `/ws/sensors` 验收连续收到两条 `sensor_data`，时间戳从 1784123425754 更新到 1784123426006。
- 已修正 `AQUAGARDEN_DATABASE_URL` 配置别名，并为当前机器配置可写运行库副本。
- 最终硬件验收：COM20 持续连接，4 帧样本中 CRC/无效帧均为 0，历史行数从 221 增至 222，`lastPersistError` 为空。
- 全量测试 4 passed，使用临时字节码缓存的 compileall 通过，git diff --check 通过。
- 已在沙箱外后台启动 FastAPI（PID 47736）；现有 front-vue 已连接 `/ws/logs`，实时帧计数达到 83。
- 用户指定实际烧录工程 `hardware/ra8p1/aquagarden`；已新增协议重审、测试、实现和硬件复测阶段。
- 已定位正式上下行协议、土壤校准公式及当前后端的泵字段偏移/控制帧不兼容问题。
- 已完整读取冻结版协议、命令表与固件编解码源码，并完成用户样本帧的字段级解释。
- 已加入真实 `0x85` 上行帧、拒绝旧 v1 布局和 5 组权威下行命令向量测试。
- 已实现完整 v2 telemetry 解码、状态/WebSocket 透传及权威 5A A5 水泵命令打包；协议测试 11 passed。
- 已用真实 COM20 验证 HTTP 与 WebSocket 完整 telemetry；新解析器连续收帧且零 CRC/无效帧。
- 已更新 README 和总结文档，以 `hardware/ra8p1/aquagarden` 冻结协议为唯一依据。
- 最终服务 PID 47296：front-vue 已连接，连续解析 62 帧，完整 telemetry 正常，11 项测试与编译检查通过。
- 已诊断登录失败：前端展示 admin 演示凭据，但 FastAPI 未初始化该用户；开始补齐环境变量驱动的管理员引导。
- 已添加管理员初始化幂等性、登录/JWT、当前用户和管理员授权测试。
- 已实现 SecretStr 管理员配置、幂等初始化、`/api/users/me` 及用户列表管理员授权；5 项认证测试通过。
- 全量 16 项测试、compileall 和 git diff --check 通过；认证配置与接口文档已更新。
- 初始管理员已写入运行库且登录返回 token；发现旧 PID 43052 占用 8090，导致新路由尚未生效，正在替换进程。
- 已替换旧进程，新服务 PID 36420；admin 登录、JWT、当前用户、管理员列表和未认证 401 均实测通过。
- 新增注册后登录集成测试，全量 17 passed，compileall 与 git diff --check 通过。
- JWT 无效凭据错误已与 front-vue 自动清理逻辑对齐；全量 18 passed。
- 最终服务 PID 42156：admin 登录、管理员角色、无效 token 401 和 COM20 实时收帧均验收通过。
